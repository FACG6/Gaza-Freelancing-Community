const connect = require('../config/db_connection');

// module.exports = (user_id) => {
//   const sql = 'update users set (firstname, lastname, city, birthday, freelancer_url, photo_url, mobile_number, ) = (select )';
// };
